# Build from source override — use with:
#   docker compose -f docker-compose.yml -f docker-compose.source.yml up -d --build
version: '3.8'

services:
  needle:
    build:
      context: .
      dockerfile: Dockerfile
    image: needle:local
    pull_policy: build
