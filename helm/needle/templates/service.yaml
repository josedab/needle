apiVersion: v1
kind: Service
metadata:
  name: {{ include "needle.fullname" . }}
  labels:
    {{- include "needle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "needle.selectorLabels" . | nindent 4 }}
